# Enable hoisting for better performance
shamefully-hoist=true

# Approve build scripts for trusted packages
enable-pre-post-scripts=true

# Explicitly approve core-js build script
# This removes the warning about ignored build scripts
